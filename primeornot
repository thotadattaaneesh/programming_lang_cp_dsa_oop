#include<iostream>
using namespace std;
int main()
{
int n,i,k,flag=1;
cout<<"enter a no";
cin>>n;
if(n==1){
        cout<<"not a prime";
        flag=0;
}
for(i=2;i*i<=n;i++){
    if(n%i==0){
        cout<<"not a prime";
        flag=0;
        return 0;
    }

}
if(flag==1) cout<<"prime";

return 0;
}
